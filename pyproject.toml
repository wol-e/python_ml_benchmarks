[tool.poetry]
name = "python_ml_benchmarks"
version = "0.1.0"
description = ""
authors = ["Wolfgang"]

[tool.poetry.dependencies]
python = "^3.8"
xgboost = "^1.4.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
